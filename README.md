# STAVCMS Local Server 0.1

Temporary build repository for the Windows x64 prototype of STAVCMS Local Server.

Stack: .NET 8, Windows x64 target. GitHub Actions publishes a self-contained win-x64 build artifact.
